services:
  - type: web
    name: guess-number
    env: dotnet
    buildCommand: dotnet publish -c Release -o ./publish
    startCommand: dotnet ./publish/GameBinary.dll
    envVars:
      - key: ASPNETCORE_URLS
        value: http://0.0.0.0:10000
